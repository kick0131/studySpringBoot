<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mysqlonazure.mapper.DiaryMapper">
    <select id="findDiaryById" resultType="com.example.mysqlonazure.dto.DiaryDTO">
        SELECT * FROM diary WHERE id = #{id}
    </select>
    <select id="findAll" resultType="com.example.mysqlonazure.dto.DiaryDTO">
        SELECT * FROM diary
    </select>

</mapper>